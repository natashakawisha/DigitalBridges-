// ===== Configuration (placeholder for a later module) =====
// This stub exists so the target project structure is in place. A future
// module will centralize environment-based configuration here (PORT,
// SESSION_SECRET, ADMIN_EMAIL, ADMIN_PASSWORD_HASH, EMAIL_USER, EMAIL_PASS).
// No secrets are wired up yet — server.js still holds its current values.
module.exports = {};
